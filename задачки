#include <iostream>
#include <string>
using namespace std;
int main() {
	setlocale(LC_ALL, "Russian");
	/*int n, num, digit, rev = 0;
	cout << "введите число" << endl;
	cin >> num;

	n = num;

	do {
		digit = num % 10;
		digit = num % 10;
		rev = (rev * 10) + digit;
		num = num / 10;
	}
		while (num != 0);
	cout << "введеное число :" << rev << endl;
	if (n == rev) {
		cout << "даннное число является палиндромом" << endl;

	}
	else {
		cout << "число на полиндром";
	}
	*/
	/*
	int sum = 1;
	int n;
	cout << "введите число" << '\n';
	cin >> n;
	for (int i = 1; i <= n; i++)
		sum = sum * i;
	cout << "факториал=" << sum;
	*/

	double sum = 0;
	int n;
	cout << "введите количество дробных чисел" << endl;
	cin >> n;
	for (int i = 1; i <= n; i++) {
		cout << "->";
		double a; 
		cin >> a;
		sum += a;

	}
	cout << sum / n << endl;
}
